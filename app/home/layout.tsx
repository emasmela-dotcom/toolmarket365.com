import type { ReactNode } from 'react'

/** Panel styles: `import './panels.css'` in page.tsx (bundled with /home route). */
export default function HomeLayout({ children }: { children: ReactNode }) {
  return children
}
