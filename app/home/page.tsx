import { HomePageClient } from '@/components/HomePageClient'

/** Tools / creator overview hub — linked from nav, `/tools` redirect, and category flows. */
export default function HomeHubPage() {
  return <HomePageClient />
}
